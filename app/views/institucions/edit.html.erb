<h1>Editar institución</h1>
<%= simple_form_for @institucion, url: institucion_path(@institucion),html: { class: 'form-horizontal' } do |f| %>
	<%= render 'fields', f: f %>
	<div class="form-actions">
		<%= f.submit 'Actualizar datos', class: 'btn btn-success' %>
	</div>
<% end %>