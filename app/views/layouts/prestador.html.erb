<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= csrf_meta_tags %>

  <%= render 'shared/shim' %>
  <%= render 'shared/livereload' %>
  <%= render 'shared/seo' %>

  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= stylesheet_link_tag "prestadors" %>
  <%= yield(:head) %>
</head>
<body id="<%= controller_name %>-<%= action_name %>">
  <div id="wrap">
    <%= render 'layouts/header' %>
    
    <div class="container">
      <div class="row">
        <!-- Sidebar
        ============================== -->
        <%= render 'layouts/prestador/sidebar' %>
        <!-- Contenido
        ============================== -->
        <div class="span8 solid">
          <%= render 'shared/flash', flash: flash %>

          <%= yield %>
        </div>
      </div><!--/row-->
    </div> <!-- /container -->


    <%= render 'shared/debug' %>
    <div id="push"></div>
  </div>
  <%= render 'layouts/footer' %>
  <!-- Javascripts
  ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "prestadors" %>
  <%= yield(:javascript) %>
</body>
</html>
