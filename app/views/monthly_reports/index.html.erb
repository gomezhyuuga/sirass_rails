<h1>Control Mensual de Horas</h1>
<p class="lead">Lista de todos los reportes que haz enviado</p>

<p>
	<%= link_to "Enviar nuevo", new_control_hora_path, class: 'btn btn-warning' %>
</p>

<table class="table table-bordered">
	<thead>
		<tr>
			<th>De</th>
			<th>A</th>
			<th>Horas</th>
			<th>Estado</th>
			<th>Acción</th>
		</tr>
	</thead>
	<tbody>
	<% @reportes.each do |r| %>
		<tr>
			<td>
				<%= r.fecha_inicio %>
			</td>
			<td>
				<%= r.fecha_fin %>
			</td>
			<td>
				<%= r.horas %>
			</td>
			<td>
				<%= r.estado_reporte.descripcion %>
			</td>
			<td>
				<%= link_to control_hora_path(r), class: 'btn btn-primary btn-mini btn-block' do %>
					<i class="icon-eye-open"></i>
					Ver
				<% end %>
			</td>
		</tr>
	<% end %>
	</tbody>
</table>