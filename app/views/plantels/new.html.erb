<% provide :title, "Agregar nuevo plantel" %>
<h1>Registrar nuevo plantel</h1>

<%= render 'shared/error_messages', object: @plantel %>

<%= simple_form_for @plantel do |f| %>
	<%= f.association :institucion, label_method: :nombre, value_method: :id, input_html: { class: 'use-select2 input-xxlarge', "data-placeholder" => "Selecciona una institución" } %>
	<%= render 'plantels/fields', f: f %>
	<div class="form-actions">
		<%= f.submit "Registrar", class: 'btn btn-success' %>
	</div>
<% end %>