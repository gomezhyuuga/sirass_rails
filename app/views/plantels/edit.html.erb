<h1>Editar plantel</h1>
<%= simple_form_for @plantel, url: admin_plantel_path(@plantel),
	html: { class: 'form-horizontal' } do |f| %>
	<%= render 'fields', f: f %>
	<div class="form-actions">
		<%= f.submit 'Actualizar datos', class: 'btn-large btn btn-success' %>
	</div>
<% end %>